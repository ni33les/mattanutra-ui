import { RESPONSIBILITY_VERSION } from "@/lib/agentic/discovery/versions";
import { AGENTIC_CONTRACT_REGISTRY } from "@/lib/agentic/contract/registry";
import { AGENTIC_CONTRACT_VERSION } from "@/lib/agentic/config";
import { PLAN_OPERATION_SCHEMAS } from "@/lib/agentic/contract/schemas";

export const CLIENT_GUIDE_URI = "mattanutra://contract/4.0.0/client-guide";
export const CONTRACT_SCHEMA_URI = "mattanutra://contract/4.0.0/schema";
export const GUIDE_ESSENTIALS = `The purchasing flow is plan, then execute, then external checkout. Polling is the only continuation method; use order(orderHandle) for payment and fulfilment. Never prefix mattanutra_dev or any server name onto short tool names. Discover info and tools/list; agree provisional nutrient targets; create a plan; review important advice and distinct options; refine explicitly; confirm the exact current option and revision with the customer; execute; open external checkout; poll order until terminal. Ready means technically purchasable, never medically approved. Nutrient limits, interactions and missing health information are advisory and require no safety acknowledgement. Unknown intake is not zero; a repetitive diet is not a diagnosis. An algae-named omega-3 target implies algae_only; vegan also requires algae omega-3, and fish DHA/EPA is wrong_source. Vitamin K2, MK-7 and Menaquinone-7 resolve through supported explicit catalogue aliases; a specific EPA or DHA target is never replaced by a broad omega-3 family. The supported dose search is a zero-new-products baseline or 1–3 whole catalogue servings per product per day; this is a computational search domain, never a medical maximum. Every objective first minimises symmetric target under/over deviation, normalized newly added dose above a known continued-dose reference for unrequested nutrients, plus 2× upper-limit excess. Estimated or unknown current doses do not create that reference. Trimmed alternative searches are incomplete, not proof no alternative exists. Keep the requested constraints and never silently substitute products. The seven short tool names are info, plan, execute, order, support, feedback, evidence. Use only these documented short tool names. Responsibility boundaries follow ${RESPONSIBILITY_VERSION}. Read evidence with the returned evidenceHandle to review supporting claims and limitations. The full schema and working examples are available through resources/read.`;
const handle = "cap_replace_with_plan_handle_from_create";
export const CLIENT_EXAMPLES = [
  { name: "discover", tool: "info", arguments: { locale: "en" } },
  { name: "create", tool: "plan", arguments: { operation: "create", idempotencyKey: "example-create-v4-0001", request: { locale: "en", destinationCountry: "TH", optimization: "lowest_cost", profile: {}, requirements: {}, targets: [{ name: "Vitamin D3", amount: 2000, unit: "IU" }], intake: [{ source: "diet", certainty: "unknown", description: "Diet varies; quantities are not known." }] } } },
  { name: "create-partial-coverage", tool: "plan", arguments: { operation: "create", idempotencyKey: "example-partial-v4-01", request: { locale: "en", destinationCountry: "TH", optimization: "lowest_cost", profile: {}, requirements: { retainProductIds: ["prd_replace_with_returned_product_id"], maxProductCount: 1 }, targets: [{ name: "Vitamin D3", amount: 2500, unit: "IU", basis: "total_daily" }], intake: [{ source: "diet", certainty: "unknown", description: "Diet varies; quantities are not known." }] } } },
  { name: "ask-customer-target-decision", tool: "plan", arguments: { operation: "revise", idempotencyKey: "example-decision-v4-1", planHandle: handle, expectedRevision: 1, requestPatch: { targets: [{ name: "Vitamin D3", amount: 2500, unit: "IU", basis: "total_daily", importance: "conditional", prerequisite: { status: "unknown", reasonCode: "customer_target_confirmation", nextAction: "The customer is deciding whether to include this provisional target." } }] } } },
  { name: "get-current-or-processing", tool: "plan", arguments: { operation: "get", planHandle: handle } },
  { name: "exclude-one-product", tool: "plan", arguments: { operation: "revise", idempotencyKey: "example-exclude-v4-001", planHandle: handle, expectedRevision: 1, requestPatch: { requirements: { excludeProductIds: ["prd_replace_with_rejected_product_id"] } } } },
  { name: "clear-product-exclusions", tool: "plan", arguments: { operation: "revise", idempotencyKey: "example-clear-v4-0001", planHandle: handle, expectedRevision: 2, requestPatch: { requirements: { excludeProductIds: [] } } } },
  { name: "answer-returned-customer-decision", tool: "plan", arguments: { operation: "answer", idempotencyKey: "example-answer-v4-001", planHandle: handle, expectedRevision: 3, answers: [{ questionId: "replace_with_returned_questionId", choice: "replace_with_returned_choice" }] } },
  { name: "select", tool: "plan", arguments: { operation: "select", idempotencyKey: "example-select-v4-001", planHandle: handle, expectedRevision: 4, optionId: "opt_replace_with_current_optionId" } },
  { name: "confirm-then-checkout", tool: "execute", arguments: { planHandle: handle, expectedRevision: 5, idempotencyKey: "example-execute-v4-01" } },
  { name: "payment-recovery", tool: "order", arguments: { orderHandle: "cap_replace_with_order_handle_from_execute" } },
  { name: "refresh-legacy-unexecuted", tool: "plan", arguments: { operation: "revise", idempotencyKey: "example-refresh-v4-01", planHandle: handle, expectedRevision: 1, requestPatch: {} } }
] as const;
export function publicContractBundle() { return { contractVersion: AGENTIC_CONTRACT_VERSION, planOperations: PLAN_OPERATION_SCHEMAS, examples: CLIENT_EXAMPLES, tools: AGENTIC_CONTRACT_REGISTRY }; }
export function clientGuideMarkdown() {
  return `# MattaNutra client interaction guide ${AGENTIC_CONTRACT_VERSION}\n\n${GUIDE_ESSENTIALS}\n\n## Plan operations\n\n- create: a full request and new stable idempotencyKey; no purchase occurs.\n- get: current plan by handle; processing uses pollAfterSeconds. No revision change.\n- revise: expectedRevision and exactly one full request (replacement) or requestPatch (object merge). Arrays replace; omission preserves in patches; [] clears; null is invalid. Patches preserve undisclosed targets and medication context. A successful edit advances revision.\n- answer: send only questionId/choice pairs returned for the current revision. Questions concern a decision, never mandatory health acknowledgement.\n- select: choose an optionId returned for the expectedRevision. It advances revision while retaining the selected basket. Option IDs describe products and dosage; always use the revision returned by the server.\n\n## Recovery and honest advice\n\nMCP targets default to basis total_daily: quantified known diet plus continued supplements plus new products. Set basis supplemental for a supplement-only target or an already calculated food gap, as the web formulation does. The response coverage and dose-fit rows state their basis. Unknown or estimated intake never becomes guaranteed coverage. Partial coverage includes every requested target; excess on one target does not cover another. Keep partial results and explain gaps; use returned questions when customer input changes the next decision. Alternatives never relax constraints or reduce any requested target's coverage. Show price, pill and dose differences; no alternative found in bounded search is not proof none exists.\n\nFor stale_revision, get current state and reapply the intended patch using its revision and a new key. Never repeat a stale selection blindly. A key reused with different input conflicts. Retry interrupted creation or execute with the SAME key and payload. An existing unpaid checkout is already a frozen order: resume it. Confirm payment only from order.paymentStatus; poll while terminal=false at pollAfterSeconds or slower. Never initiate another charge to recover payment.\n\nLegacy unexecuted plans explicitly refresh with an empty requestPatch. Existing checkout/order identities and receipts remain valid. Known, estimated and unknown intake are distinct; food descriptions need no exact labels to explore. Omitted currentSupplements means unknown and [] explicitly reports none. Retained inventory daysRemaining must be positive; omit unknown duration, and remove an exhausted inventory entry explicitly before replanning. Omitted or empty intake does not establish zero food intake: use an explicit known amount of 0 only when actually known. Do not report the same product and nutrient pair in both currentSupplements and a current-source intake observation. Savings need equivalent coverage, period and delivered cost; unavailable comparisons are labelled.\n\n## Partial coverage and an explicit customer choice\n\nThe separate create-partial-coverage example uses a provisional test target, not a suggested personal dose. From the initial response, choose the returned Vitamin D3 product only if its requestedNutrients amount divided by servingsPerDay is 1000 IU per serving. Retain that returned product ID and limit the basket to one product; copy the already disclosed profile, intake and medication context into this separate full create request. For the 2500 IU fixture target, the supported whole-serving search returns 2000 IU: the nutrient row shows 80% dose coverage and a 500 IU gap. That target remains partially covered; this is not a claim that one whole target is completed. The plan can be ready without any question or acknowledgement, and unknown dietary intake remains unknown. Do not execute this separate diagnostic plan.\n\nTo exercise answer independently of the partial gap, the ask-customer-target-decision example explicitly records that the customer is deciding whether to include the same provisional target. This is a customer preference, not a medical prerequisite. Send the complete replacement target array shown while preserving its amount, unit and basis. The connector returns a current question and choices. In this fixture the customer then confirms inclusion: select the returned choice labelled "Mark the prerequisite satisfied" and send its actual questionId and choice with the newly returned revision. Do not invent a question ID, choice, or safety acknowledgement. The answered plan returns to ready with the same 80% nutrient-row coverage, 500 IU gap, retained product and medication context.\n\n## Executable request templates\n\nExamples use response placeholders for opaque handles, IDs, revisions and returned answers. The scripted client substitutes these from prior responses; no internal catalogue or database access is needed.\n\n${CLIENT_EXAMPLES.map(example => `### ${example.name}\n\n\`\`\`json\n${JSON.stringify({ method: "tools/call", params: { name: example.tool, arguments: example.arguments } }, null, 2)}\n\`\`\``).join("\n\n")}\n`;
}
export const CONTRACT_RESOURCES = [
  { uri: CLIENT_GUIDE_URI, name: "MattaNutra v4 client guide", mimeType: "text/markdown", description: "Workflow, complete operation semantics, advice and payment recovery examples." },
  { uri: CONTRACT_SCHEMA_URI, name: "MattaNutra v4 complete contract", mimeType: "application/schema+json", description: "Input and response contracts for all seven tools and individual plan operations." }
] as const;
export function readContractResource(uri: string) {
  const resource = CONTRACT_RESOURCES.find(item => item.uri === uri);
  return resource ? { contents: [{ uri, mimeType: resource.mimeType, text: uri === CLIENT_GUIDE_URI ? clientGuideMarkdown() : JSON.stringify(publicContractBundle(), null, 2) }] } : null;
}
