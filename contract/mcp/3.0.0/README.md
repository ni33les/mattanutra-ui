# MattaNutra MCP contract 3.0.0

Public tools: `info`, `plan`, `execute`, `order`, `support`, `feedback`.

Canonical TypeScript schemas live in `lib/agentic/contract/schemas.ts`. Host adapters in `lib/agentic/adapters/` map transport only.
