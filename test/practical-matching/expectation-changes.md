# Practical matching expectation ledger

Release control: `ab102ab3930cbaea0594847152ac010bcae722ca` (DEV and UAT).

- PRACTICAL-SCORE-01 replaces dose-first default ranking with the approved shared weighted penalty. Raw dose arithmetic, clinical reference content and prices do not change.
- Numerical preferences remain advisory. Profiles affect ranking only; no new eligibility conditions, locks or execution queues are permitted.
- Historical results remain historical. Every later changed assertion must identify its replacement behaviour and independent arithmetic here.
- The reported assessment is in DEV, not UAT. Evidence exports permit only matching inputs, catalogue/reference facts, quantities, preferences and generated matching results; contact and capability data are excluded.

## Contract 8 maintained expectations

- Advisory-dose-fit bounded-frontier case now asserts deterministic traversal and the same expansion allowance per selected profile. Identical dose-first winners across profiles would contradict the approved weighted policy.
- ADV6-CORE-04 compares the actual purchasable one-product choices, including fallback when no purchase wins. Unknown quantities keep null actuals and an uncertainty penalty; the `simpler` role still prefers verified counts. Unknown is not required to rank behind arbitrarily large known overruns.
- V5-COUNT-02 keeps the two-product zero-preference purchase choice available instead of requiring it as the default.
- V5-DOSE-01, V5-INTAKE-01–03, V5-SEARCH-01/05, V5-REPAIR-01/03/04 and ADV6-REPAIR-01 retain their exact SKU, quantity, exposure and price assertions on the returned `closest_dose` witness. Weighted default assertions live in PRACTICAL-SCORE/SEARCH/ORACLE. An absent or worse exact-dose witness remains a regression, not an expectation to weaken.
- V5-REPAIR-02/05 and V5-SEARCH-03 require expansion to preserve the incumbent's overall score under the selected profile. Catalogue ordering and deterministic results remain asserted.
- V5-OPTION-02 now recommends the existing 99.9%-fit cheap choice under `lowest_cost`, preserving the exact-fit alternative, original prices and 0.1 gap. V5-OPTION-03 retains the exact two-product basket while allowing the simpler partial choice to be recommended.
- Phase-2/QA-GOLD M-01/M-05 retain the original exact-dose golden products, quantities and ฿610 price as an exposed trade-off. Phase-3's above-budget exact SKU remains selectable, without requiring a very expensive basket to be recommended.
- V5-CHECKOUT-01/02 and V5-CHECKOUT-PG-01 call the explicitly named mutation-fence method when checking checkout publication. PRACTICAL-CHECKOUT-PG-01 separately proves the preview method does not lock. Existing frozen-payment expectations remain unchanged.
- The numeric-preference browser fixture explicitly selects its returned exact-dose basket above zero preferences; it does not require that basket to be the default. Original C/D3 labels and ฿23/฿17 fixture prices remain unchanged.
- New PRACTICAL-SEARCH-04 fixture uses a preference of two for a known two-pill option, isolating an unknown-as-zero bug rather than incorrectly demanding that uncertainty outweigh every known overrun. No historical fixture changed.
- The isolated PostgreSQL RED setup initially rejected an incomplete environment (DB_URL missing). The subsequent valid isolated run reproduced SQLSTATE 55P03 on a held catalogue writer; only that run counts as the contention RED.

- DISC-MCP-07/10, AG72-CARD-01, AXR-SPEC and current-contract-lock now pin the reviewed **8.0.0** snapshot. PAY-SCHEMA-01 permits only its reviewed schema-size delta while preserving the 2 KB descriptor-overhead allowance and complete schema validation. Historical 7.2.4 snapshots remain unchanged; the v4 resource case now checks all 22 resources including the preserved 7.2.4 pair and current v8 pair.
- The in-memory conversation presentation helper represents a current committed record, so its version follows the active contract. The historical D3 JSON remains unchanged. Explicit obsolete-result and frozen-checkout tests continue using their historical versions.
- The maintained v4 contract journey exposed a response-compaction defect for null score preferences. The fix preserves the typed overallScore object, including null preferred/scale/actual fields; validation and public details keep those distinctions.
- V5-REPAIR-01/03 and ADV6-REPAIR-01 exposed lost raw-dose continuations under practical scoring. Search retains raw-dose and exact-target completion bases alongside profile leaders. Supported quantity probes share the original attempt budget and their continuations survive beam/repair reduction.
- ADV6-REPAIR-01 now discovers the same three exact SKU/quantity lines by retaining both required proposal groups before adding their complement. Its deterministic line vector is a → b → complement; all amounts, price (300 minor), three pills, three products, coverage and eligibility assertions are unchanged. Baseline evidence retains the previous a → complement → b traversal.
- V5-PUBLISH-01's 1,000 IU fixture now has a legitimate no-purchase recommendation under practical scoring. The test still asserts completed durable publication, explicit stale presentation after the catalogue changes, and unchanged saved status after GET. No publication invariant is relaxed.
- Customer-value implementation/r2/r3/r4 packs pin `practical-matching-1` instead of the previous `flexible-dose-fit-9`. Their frozen inputs, all case IDs, financial assertions and paired semantic comparisons are retained.
- AE-01's 180-word descriptor budget remains unchanged; the shared overview was shortened rather than weakening the budget.
- PRACTICAL-SCORE-10 independently reproduced an extra 0.25 charge for unavailable monthly prices. Only the approved administration uncertainty charge remains; missing monetary assessments stay explicitly unavailable. The profile hash includes the numeric importance factors as well as their names.

## Maintained regression repairs

- Acceptance-3 is preserved against unchanged `004dd86a` with its complete failures; it is not a green attestation. Active contract assertions now pin 8.0.0, including the tools-only AX client; historical resources and previous evidence remain untouched.
- AXR-SRCH-01 exposed chunk-dependent candidate retention. Candidate reduction now uses fixed expansion-count boundaries; the original deep equality and attempt assertions remain unchanged. PRACTICAL-SEARCH-12 exposed an optional-target veto when required exposure was identical. A required-priority override now requires an actual protected nutrient/reference improvement. Existing OPT/COST/SAFE money and coverage assertions remain intact.
- PRACTICAL-API-08 exposed a separate compact search policy. Compact now differs only in the approved fewest_pills weights; the retained compatibility config aliases the common web config. PRACTICAL-API-09 verifies complete web/MCP score equality with identical resolved inputs.
- PRACTICAL-SCORE-11 pins the requested fallback semantics: small pill/product/price objectives apply only while the corresponding preference is absent. Serving burden and administration uncertainty always apply. The independent finite interior optimum is now 0.7 servings (penalty 0.125 + 0.16 + 0.05 + 0.005 = 0.34), versus 0.6 costing 0.25 + 0.04 + 0.05 + 0.005 = 0.345. SEARCH-03/08/11 and ORACLE-01 retain their prices and exact arithmetic with this approved formula. SEARCH-09 removes only the fallback price term from its exact fraction, producing 7/45. Historical offline experiment outputs, including their 0.6 optimum, are unchanged.
- Empty practical winners use `empty_practical_fit`; only an empty raw-dose winner uses `empty_closest_fit`. Both preserve their purchase fallbacks and truthful recovery. Three locales are covered by PRACTICAL-WEB-06.
- Execute/reliability fixtures whose practical recommendation is empty explicitly select the original returned purchase option before asserting checkout/replay invariants. No price, payment identity or recovery assertion is relaxed. CV/recovery publication fixtures assert their completed no_purchase state and still fence stale publication.
- Phase-1/2/3/4/6, AX retained-complement and WEB5/6 dose witnesses assert their original exact products, quantities, doses, pills and prices on returned closest_dose options. Practical selection is asserted separately. An absent exact witness remains a failure. Eight-product and high-serving options remain selectable above preferences.
- M721-ROUTINE keeps a cheaper one-serving incidental choice and independently proposes the original ten-serving exact basket, proving that physical eligibility remains advisory above all numerical preferences. Unknown administration may beat a large verified overrun only with null actuals, explicit missing measurements and its 0.25 uncertainty penalty; it is never scored as a verified zero. The D3 closest-dose witness stays available. M722 retains the dedicated selected routine and checks the useful cheaper incidental choice without demanding a dominated ten-serving default alternative.
- M721-GOLD selects the returned exact-dose alternative before testing above-preference selection; the zero-budget initial result may be no_purchase. Timing and payload budgets remain unchanged. Overview medication assertions follow the equally explicit concise text “accepted inputs, not interaction coverage”; unassessed-code checks remain intact.
- The frozen 90-day retailer comparison retains all prices: equal fit now prefers the cheaper first order, making the original 40,000-minor basket selected and the 43,000-minor basket an explicit +3,000 alternative.

- Acceptance-3 reached the 600-second file-container timeout while serializing all 18 PAY-AX-01 journeys, after ten individual cases passed. The same 18 cases are partitioned into six per locale, with the existing 120-second individual deadlines and 600-second file ceiling unchanged. Shared assertions live in journey-case.ts; each registered case remains attached to a discovered .test.ts file, with explicit six-case inventory entries. No assertion, locale, price, settlement check or comparison was removed.

- PAY-TRANSPORT-03 now proves an unexecuted 7.0 result receives contract_refresh_required before selection, remains historical, refreshes through the existing revision flow, and recovers an existing frozen checkout after a historical contract marker is restored. Its original frozen items, quantities and line totals remain exact assertions.

- The complete unchanged-source diagnostic executed all 406 selected files and preserved 87 non-integration failed cases/container results plus seven PostgreSQL failures. PAY-AX's container timeout left eight named locale cases without completion; the diagnostic is explicitly unsuccessful. No green proof was issued.
- The remaining A4 regression was real: the 296 mg calcium completion base was explored, then discarded before its D3 complement could be added. Retention now keeps bounded per-target bases without existing target excess, and the final addition phase preserves those bases and distributes its remaining attempts across products. AXR-SRCH-02 retains its original 4/300 loss and 91,900-minor price witness; A5 keeps 5/100 and 70,300. No extra attempts or eligibility rule were introduced.
- PostgreSQL catalogue-publication/restart fixtures now assert their completed no_purchase result, preserving the held-writer barrier, revision count, terminal receipt and stale projection checks. Atomic checkout fixtures explicitly select a returned purchase before exercising their unchanged concurrent transition/frozen-order assertions. PAY-POLL-03 uses the current contract marker to isolate catalogue freshness; PAY-TRANSPORT-03 covers intentional historical-contract refresh separately.
- PRACTICAL-RELEASE-04 caught the compiled-artifact dependency of real HTTP-worker integration tests. The same single production build now precedes the complete inventory; typecheck and release-diff lint run first. Individual worker deadlines and identity validation are unchanged. The other work packages retain their existing stage order.
