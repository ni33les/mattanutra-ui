# Reviewed changes

- WEB-JOURNEY-01/02 replace the heuristic six-to-twelve shortlist. Unknown means null; completed visible ingredients use the same revision and locale as reveal. Set-aside counts are not invented. HealthScore arithmetic and complete-advice gates remain unchanged.
- HealthScore panel/static tests now require current formula copy rather than obsolete AI-generated shortlist prose. Personalised narrative remains AI-written. Fixed model references are no longer described as observed customer averages/rankings.
- WEB-TIDY-03 and PRACTICAL-API-09 use web product multiplier 5 instead of 1.25; other objective/health terms remain unchanged. The unchanged old source and RED evidence remain outside the checkout. WEB-TIDY-04 still checks modest-gap trade-offs and nonblocking eligibility.
- Foundation is core; explicit add-on/optional categories are optional; other targets remain required. Requested amounts and all target coverage denominators are preserved.
- WEB-JOURNEY-05 protects pair consolidation with supported whole quantities, within the existing attempt budget. MCP traversal is unchanged; existing cursor readers accept the optional internal job fields.
- Frozen reported fixture preserves prices and the original bad unit. Candidate tests apply only the reviewed B12 correction to a clone. JSON whitespace was compacted without altering any frozen values. No historical result or price was rewritten.
- The reported six-product outcome is a fixture regression, not a product-count limit. Numerical preferences remain advisory.
- The first reported-fixture check incorrectly required 90% D3 coverage while its measured supported-quantity trade-off supplies 1,400/2,000 IU (70%). Preserve that failure and assert the exact dose and 30% shortfall instead; Omega-3, theanine and creatine retain the 90% minimum assertions. Product reduction is not presented as equal coverage.
