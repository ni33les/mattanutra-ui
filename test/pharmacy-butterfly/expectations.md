# One butterfly flight

The user explicitly replaces the opening hop/tap choreography and its later loop with one graceful continuous flight. The supplied attachment and its historical geometry remain frozen; they are no longer the runtime flight requirement.

- PHARM-MOTION-01–03 now check one continuous curve and a distance-based trail rather than timed question taps and the former first-phase-only path.
- PHARM-FLIGHT-01–04 retain pending motion, exact tip attachment, landing continuity and viewport bounds using the single trajectory.
- Browser motion measurements span the old phase boundaries and check the visible trail throughout. Steady trail length is 180 px on the mobile fixture and 300 px on desktop; rendering must not depend on animation-frame count or travel direction.
- PHARM-COMBINE responsive cases retain rain/layout/readiness assertions and replace old tap assertions with continuous flight.
- Completed plans remain still. Real readiness reveals results immediately while the sprite lands. Hidden tabs, reduced motion, navigation, order state and ordinary web pages retain their behaviour.

No prices, catalogue facts, API contracts or matching inputs change. Earlier evidence is retained outside the repository.

## Magic-dust follow-up

The user replaces the continuous line with soft magic-dust sparkles. PHARM-MOTION-03 now checks time-based emission at the leaf tip, gentle drift, fading, frame-rate independence and a fixed 48-particle pool. Browser checks replace line-length assertions with visible dust, near-tip emission, no drawn line and full cleanup. Flight coordinates and landing assertions remain unchanged. Previous line-trail results remain historical evidence.

## White stardust and stable questionnaire sections

Requested follow-up: denser white star dust, slightly larger pharmacy ingredient cards, and a stable first question after each section overlay. The flight geometry, landing, readiness and order semantics are unchanged.

- PHARM-MOTION-03 and mobile/desktop browser checks now require a bounded 96-particle pool and at least 50 visible particles during steady flight (previously 48/20). Emission remains frame-rate independent and attached to the sprite.
- PHARM-COMBINE reload also checks ingredient cards are at least 72 px tall. The original responsive column counts remain unchanged.
- QUIZ-MOTION checks the real section boundary with English ordinary and Thai/Chinese pharmacy routes: the new question settles under the overlay before it fades, no competing smooth scrolls, preserved answer focus and reduced motion.
- RED evidence against 48718f39 is preserved at `/root/.codex/deploy/pharmacy-stardust-20260919/red-node` and `red-browser`: one density failure and five rendered presentation failures. No historical cases or prices were removed.

The pharmacy-only guide poses now use separate, transparent empty-handed variants. PHARM-WAIT-01 still verifies the original attachment and all original mascot/leaf assets byte-for-byte; its rendered-source assertion now checks the three new pharmacy paths. The original assets remain available unchanged to all other pages. The asset-reference RED is preserved in `red-prop`; manual review verifies removed props and preserved mascot poses. The question motion assertion concerns the row/bubble entrance, not Nong's intentional idle bob.
